# Hugging Face Spaces requirements (Gradio SDK)
# Compatible with Python 3.10

numpy<2.4
scipy
pandas
scikit-learn>=1.3,<1.6  # Compatible with Python 3.10, model trained with 1.8.0 loads fine
librosa>=0.10
soundfile
opensmile>=2.5
joblib>=1.3
shap>=0.44
gradio==4.44.0
huggingface-hub>=0.19,<0.27  # Compatible with gradio 4.44.0
# gradio 4.44.0 is a Sept-2024 release that declares only `fastapi<1.0` and
# `pydantic>=2.0`, so pip otherwise installs today's transitive deps against it.
# These two pins hold the web stack at versions contemporaneous with it.
pydantic==2.10.6   # 2.11+ emits `additionalProperties: true` for bare dict fields
                   # (gradio FileData.meta); gradio_client 1.3.0 get_type() then does
                   # `"const" in True` -> TypeError, crashing / via get_api_info().
fastapi==0.115.0   # constrains starlette to <0.39, which still accepts the deprecated
                   # TemplateResponse(name, context) signature gradio 4.44.0 calls.
                   # starlette 1.0.0 removed it -> TypeError: unhashable type: 'dict'.
