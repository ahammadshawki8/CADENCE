# Hugging Face Spaces requirements (Gradio SDK)
# Note: This is for the root level. Backend dependencies are in backend/requirements.txt

gradio>=4.0
numpy<2.4
scipy
pandas
scikit-learn>=1.3,<1.6  # Compatible with Python 3.10
librosa>=0.10
soundfile
opensmile>=2.5
joblib>=1.3
shap>=0.44
huggingface-hub>=0.20,<0.30  # Fix compatibility with Gradio
